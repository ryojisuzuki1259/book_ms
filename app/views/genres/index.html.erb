<h1>ジャンル一覧画面</h1>

<%= form_with model:@genre, local:true do |f|%>
    <%= f.label :"ジャンル名" %>
    <%= f.text_field :name %>
    <%#= f.hidden_field :is_active, :value => "true" %>
    <div><%= f.submit "登録する"%></div>
<% end %>

<div id="genre-index"><%= render "genres/partial_index", genres: @genres %></div>

<%= link_to "書籍一覧画面へ戻る", books_path %>