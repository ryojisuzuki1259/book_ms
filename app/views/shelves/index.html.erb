<h1>本棚一覧画面</h1>

<%= form_with model:@shelf, local:true do |f|%>
    <%= f.label :"本棚名" %>
    <%= f.text_field :name %>
    <%#= f.hidden_field :is_active, :value => "true" %>
    <div><%= f.submit "登録する"%></div>
<% end %>

<div id="shelf-index"><%= render "shelves/partial_index", shelves: @shelves %></div>

<%= link_to "書籍一覧画面へ戻る", books_path %>